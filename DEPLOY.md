# Инструкции по деплою

Этот проект можно задеплоить на несколько платформ. Ниже описаны лучшие варианты.

## 🚀 Рекомендуемые платформы

### 1. Railway (⭐ Рекомендуется)

**Плюсы:**
- Простой деплой через GitHub
- Встроенная PostgreSQL база данных
- Бесплатный план с $5 кредитами в месяц
- Автоматический HTTPS
- Переменные окружения из интерфейса

**Шаги:**

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите ваш GitHub репозиторий
3. Создайте новый проект → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js проект
6. Добавьте PostgreSQL базу данных:
   - Нажмите "+ New" → "Database" → "Add PostgreSQL"
   - Railway автоматически создаст переменную `DATABASE_URL`
7. Добавьте переменные окружения:
   - Settings → Variables → Add Variable
   - `SESSION_SECRET` = случайная строка (например, сгенерируйте через `openssl rand -base64 32`)
   - `NODE_ENV` = `production`
   - `BASE_URL` = ваш домен Railway (например, `https://your-app.up.railway.app`)
8. Railway автоматически запустит:
   - `npm install`
   - `npm run build`
   - `npm run start`
9. После деплоя примените миграции:
   - Settings → Deployments → View Logs
   - Или через Railway CLI: `railway run npm run db:push`

**Стоимость:** Бесплатно до $5/месяц, затем ~$5-20/месяц

---

### 2. Render

**Плюсы:**
- Бесплатный план (с ограничениями)
- Встроенная PostgreSQL
- Автоматический деплой из GitHub
- HTTPS включен

**Шаги:**

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте новый "Web Service":
   - Repository: выберите ваш репозиторий
   - Environment: `Node`
   - **Install Command:** `npm install` (убедитесь, что devDependencies устанавливаются)
   - **Build Command:** `npm run build`
   - **Start Command:** `npm run start`
   - Instance Type: Free (или выберите платный)
   - **Важно:** Если сборка не работает, попробуйте Build Command: `npm install && npm run build`
4. Добавьте PostgreSQL базу данных:
   - Dashboard → "New +" → "PostgreSQL"
   - Выберите "Free" план
   - Render автоматически создаст переменную `DATABASE_URL`
5. В настройках Web Service добавьте переменные окружения:
   - Environment → Add Environment Variable
   - `SESSION_SECRET` = случайная строка
   - `NODE_ENV` = `production`
   - `BASE_URL` = ваш домен Render (например, `https://your-app.onrender.com`)
6. После деплоя примените миграции:
   - В разделе "Shell" выполните: `npm run db:push`

**Стоимость:** Бесплатно (с ограничениями: спящий режим после 15 минут бездействия), от $7/месяц для всегда активного

---

### 3. Fly.io

**Плюсы:**
- Бесплатный план с хорошими лимитами
- Глобальная сеть CDN
- PostgreSQL через отдельный сервис

**Шаги:**

1. Установите Fly CLI: `npm install -g @fly/cli`
2. Зарегистрируйтесь: `fly auth signup`
3. Создайте приложение: `fly launch`
4. Создайте PostgreSQL базу данных:
   ```bash
   fly postgres create --name your-app-db
   fly postgres attach your-app-db
   ```
5. Создайте файл `fly.toml` (если не создан автоматически):
   ```toml
   app = "your-app-name"
   primary_region = "iad"

   [build]
     builder = "paketobuildpacks/builder:base"

   [env]
     NODE_ENV = "production"
     PORT = "8080"

   [[services]]
     internal_port = 8080
     protocol = "tcp"

     [[services.ports]]
       handlers = ["http"]
       port = 80
       force_https = true

     [[services.ports]]
       handlers = ["tls", "http"]
       port = 443
   ```
6. Добавьте секреты:
   ```bash
   fly secrets set SESSION_SECRET=your-secret-key
   fly secrets set BASE_URL=https://your-app.fly.dev
   ```
7. Деплой: `fly deploy`
8. Примените миграции: `fly ssh console -C "npm run db:push"`

**Стоимость:** Бесплатно до определенных лимитов, затем от ~$5/месяц

---

### 4. DigitalOcean App Platform

**Плюсы:**
- Простой интерфейс
- Встроенная PostgreSQL
- Автоматический деплой из GitHub

**Шаги:**

1. Зарегистрируйтесь на [digitalocean.com](https://digitalocean.com)
2. Создайте новый App:
   - Connect GitHub репозиторий
   - Выберите репозиторий и ветку
3. Настройте Build & Run:
   - Build Command: `npm install && npm run build`
   - Run Command: `npm run start`
   - HTTP Port: `5000`
4. Добавьте PostgreSQL базу данных:
   - Resources → Add Database → PostgreSQL
   - Выберите план (от $12/месяц)
5. Добавьте переменные окружения:
   - Settings → App-Level Environment Variables
   - `SESSION_SECRET` = случайная строка
   - `NODE_ENV` = `production`
   - `BASE_URL` = ваш домен DigitalOcean
6. После деплоя примените миграции через консоль

**Стоимость:** От $5/месяц (без базы данных), от $12/месяц с PostgreSQL

---

## 📋 Общие шаги для всех платформ

### Переменные окружения

Убедитесь, что установлены следующие переменные:

```env
DATABASE_URL=postgresql://user:password@host:port/database
SESSION_SECRET=ваш-секретный-ключ-минимум-32-символа
NODE_ENV=production
BASE_URL=https://ваш-домен.com
```

### Генерация SESSION_SECRET

```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

### Применение миграций

После первого деплоя обязательно выполните:

```bash
npm run db:push
```

Это создаст все таблицы в базе данных.

---

## 🔧 Настройка email для production

Для отправки email подтверждений в production настройте SMTP:

1. Добавьте переменные окружения:
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_SECURE=false
   SMTP_USER=ваш-email@gmail.com
   SMTP_PASS=ваш-пароль-приложения
   SMTP_FROM=noreply@yourdomain.com
   ```

2. Для Gmail:
   - Включите "Двухэтапную аутентификацию"
   - Создайте "Пароль приложения" в настройках Google аккаунта
   - Используйте этот пароль в `SMTP_PASS`

---

## 🎯 Рекомендация

**Для начала:** Railway или Render (бесплатные планы)
**Для продакшена:** Railway или DigitalOcean (больше контроля)

---

## ❓ Troubleshooting

### Ошибка "tsx: not found" при сборке на Render

Если вы видите ошибку `sh: 1: tsx: not found`:

1. **Решение 1:** Убедитесь, что Build Command использует `npm install && npm run build` (уже исправлено в package.json)
2. **Решение 2:** Если проблема сохраняется, измените Build Command на: `npm ci && npm run build`
3. **Решение 3:** Убедитесь, что `tsx` находится в `devDependencies` (уже есть)
4. **Решение 4:** Попробуйте использовать прямой путь в Build Command: `npm install && node_modules/.bin/tsx script/build.ts`

### База данных не подключается
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных создана и запущена
- Проверьте firewall настройки платформы

### Миграции не применяются
- Выполните `npm run db:push` вручную через консоль платформы
- Проверьте права доступа к базе данных

### Сессии не работают
- Убедитесь, что `SESSION_SECRET` установлен и достаточно длинный
- Проверьте, что `BASE_URL` указан правильно (с https://)

### Email не отправляется
- Проверьте SMTP настройки
- В development режиме email логируется в консоль
- Для production настройте реальный SMTP сервер

